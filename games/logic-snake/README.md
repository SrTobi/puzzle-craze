# Logic Snake

The second Puzzle Craze game. Currently a coming-soon page; gameplay is not implemented yet.

- URL: `/games/logic-snake/`
- `index.html` — page metadata and entry point
- `src/App.tsx` — placeholder; replace with the game as development progresses
- `src/styles.css` — styles specific to Logic Snake
- `src/main.tsx` — mounts the app using the shared bootstrap and base styles

Run the root development, test, and build commands from the repository root. Keep future rules, levels, assets, and tests in this directory. Import reusable components and utilities from `shared/`.
